# Реестр дополнительных заданий

Этот документ содержит дополнительные задания, которые рассматриваются для включения в лабораторные работы. Он не импортируется в методичку и используется для разработки, стендовой проверки и фиксации результатов.

Задание переносится в текст лабораторной только после полного прогона на целевом стенде. До этого в соответствующей лабораторной сохраняется только заголовок «Дополнительные задания».

## Статусы проверки

| Статус | Значение |
|---|---|
| **Не проверено** | Задание сформулировано, но команды и полный сценарий на стенде не выполнялись. |
| **Проверены команды** | Основные команды или отдельные этапы работают, но полный студенческий сценарий и его длительность не проверены. |
| **Проверено полностью** | Задание выполнено от начала до конца на целевом стенде, зафиксированы время, результат и очистка после эксперимента. |
| **Требует исправления** | Во время проверки обнаружен блокер, неоднозначность или превышение допустимого времени. |

Для статуса «Проверено полностью» обязательны:

- модель платы, версия образа и используемое оборудование;
- точная последовательность действий без скрытых подготовительных шагов;
- фактический результат и критерий успешности;
- продолжительность выполнения студентом или проверяющим;
- минимум одна типовая ошибка и способ восстановления;
- подтверждение, что после задания не остались процессы, пользователи, ключи или изменённые настройки.

## Проверочный стенд

Проверка лабораторных №1–5 выполнялась в следующем окружении:

| Компонент | Значение |
|---|---|
| Плата | Lichee RV Dock, Allwinner D1 |
| Система | ALT Regular, build `20250612` |
| Ядро | Linux `6.16.0`, `riscv64` |
| Сеть платы | `wlan0`, IPv4, рабочий маршрут и DNS |
| UART | `/dev/ttyUSB0`, FTDI FT232R, 115200 8N1 |
| Права UART | группа `uucp` |
| Терминал | `tio 3.9` |
| SSH | OpenSSH, служба `sshd`, TCP-порт 22 |
| Кросс-компилятор | `riscv64-linux-gnu-gcc 14.3.1` |
| Эмулятор | `qemu-riscv64-static 10.2.2`, зарегистрирован `binfmt_misc` |
| Инструменты ELF | `file 5.47`, `riscv64-linux-gnu-readelf 2.43` |
| Исходники ядра | Linux `6.16.0`, пакет `kernel-source-6.16-0-alt1` |
| Сборка ядра | GNU Make 4.4, внешний каталог `O=...`, до 8 заданий |
| OpenSBI | commit `3593a5facc4c6938b90429a6973ba9ee21fc5899` |
| U-Boot | `d1-wip`, commit `2e89b706f5c956a70c989cd31665f1429e9a0b48` |
| Инструменты загрузки | SWIG 4.4.1, DTC 1.7.2, GNU Make 4.4, до 8 заданий |
| Rootfs для лабораторной №5 | ALT Regular `20260316`, riscv64, XZ |

IP-адрес, пароли, серийные номера и другие изменяемые или чувствительные данные в реестре не фиксируются.

## Сводная таблица

| ID | Лабораторная | Краткое название | Статус |
|---|---:|---|---|
| `1.1` | 1 | Карта загрузки | Проверено полностью |
| `1.2` | 1 | Паспорт платы | Проверено полностью |
| `1.3` | 1 | Сравнение Lichee и рабочего компьютера | Проверено полностью |
| `1.4` | 1 | Устойчивое имя UART | Проверено полностью |
| `1.5` | 1 | Наблюдение нагрузки | Проверено полностью |
| `1.6` | 1 | Диагностика неверной скорости | Проверено полностью |
| `2.1` | 2 | Карта сетевого пути | Проверено полностью |
| `2.2` | 2 | Двусторонняя передача | Проверено полностью |
| `2.3` | 2 | Псевдоним SSH | Проверено полностью |
| `2.4` | 2 | Удалённая команда | Проверено полностью |
| `2.5` | 2 | SSH-туннель | Проверено полностью |
| `2.6` | 2 | Диагностика соединения | Проверено полностью |
| `3.1` | 3 | Инспектор ELF | Проверено полностью |
| `3.2` | 3 | Надёжный CLI | Проверено полностью |
| `3.3` | 3 | Статическая и динамическая сборки | Проверено полностью |
| `3.4` | 3 | Воспроизводимая сборка | Проверено полностью |
| `3.5` | 3 | Системные вызовы под QEMU | Проверено полностью |
| `3.6` | 3 | Численная сопоставимость | Проверено полностью |
| `4.1` | 4 | Атлас артефактов ядра | Проверено полностью |
| `4.2` | 4 | Карта зависимостей Kconfig | Проверено полностью |
| `4.3` | 4 | Полная и сокращённая конфигурации | Проверено полностью |
| `4.4` | 4 | Встроенный драйвер или модуль | Проверено полностью |
| `4.5` | 4 | Внешнее дерево сборки | Проверено полностью |
| `4.6` | 4 | Чистая и инкрементальная сборки | Проверено полностью |
| `5.1` | 5 | Паспорт комплекта загрузки | Проверено полностью |
| `5.2` | 5 | Аудит DTB платы | Проверено полностью |
| `5.3` | 5 | Дополнение шаблона Extlinux | Проверено полностью |
| `5.4` | 5 | Самостоятельный и пакетный U-Boot | Проверено полностью |
| `5.5` | 5 | Аудит архива rootfs | Проверено полностью |
| `5.6` | 5 | Воспроизводимая сборка U-Boot | Проверено полностью |

## Лабораторная №1

### 1.1. Карта загрузки

**Идея:** запустить `tio` с временными метками и записью журнала, перезагрузить плату и найти в журнале работу загрузчика, запуск ядра Linux и запуск пользовательского пространства.

```bash
tio -b 115200 -t -L --log-file boot.log /dev/ttyUSBX
```

**Ожидаемый результат:** для каждого из трёх этапов приведена характерная строка и объяснено, что к этому моменту уже работает.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** журнал размером 58 КБ содержит `U-Boot`, `Starting kernel`, `Linux version 6.16.0`, `Run /sbin/init`, достижение `multi-user.target` и приглашение `login:`. От перезапуска до приглашения прошло около 75 секунд.

**Типовая ошибка и восстановление:** без псевдотерминала `tio` переходит в неинтерактивный режим и сразу отключается; в обычном терминале команда работает штатно. Временный журнал удаляется после анализа.

### 1.2. Паспорт платы

**Идея:** составить таблицу с архитектурой, числом процессоров, версией ядра, RAM, разделами, файловыми системами, сетью, именем системы и часовым поясом.

**Ожидаемый результат:** для каждого значения указаны команда, фактическое значение и краткая интерпретация.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** за 31 секунду через UART получены `riscv64`, один CPU, 472 МиБ RAM, разделы BOOT/ROOTFS, ядро 6.16.0, `wlan0`, имя системы и часовой пояс `Europe/Saratov`. Типовая ошибка — попытка использовать отсутствующий `fdisk`; восстановление — `lsblk -f`. Настройки не изменялись.

### 1.3. Сравнение Lichee и рабочего компьютера

**Идея:** выполнить `uname -a`, `lscpu`, `free -h`, `lsblk -f` и `ip -brief address` на обеих системах, затем объяснить не менее пяти различий.

**Ожидаемый результат:** различия разделены на архитектурные, ресурсные и связанные с назначением системы.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** за 31 секунду подтверждены различия архитектуры (`riscv64`/`x86_64`), числа CPU (1/12), RAM (472 МиБ/15 ГиБ), накопителя (microSD/NVMe), набора интерфейсов и назначения систем. Типовая ошибка — смешивание выводов двух машин; восстановление — подписывать каждый блок именем и архитектурой узла. Настройки не изменялись.

### 1.4. Устойчивое имя UART

**Идея:** определить модель и серийный номер USB-UART-преобразователя и сравнить `/dev/ttyUSBX` с путём из `/dev/serial/by-id/`.

```bash
udevadm info --query=property --name=/dev/ttyUSBX
ls -l /dev/serial/by-id/
```

**Ожидаемый результат:** студент объясняет, почему символьная ссылка по идентификатору устойчивее динамического номера `ttyUSBX`.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** менее чем за секунду определены FTDI FT232R, серийный идентификатор и ссылка `/dev/serial/by-id/`, ведущая на `/dev/ttyUSB0`. Типовая ошибка — указать несуществующий `ttyUSBX`; восстановление — повторно просмотреть каталог `by-id`. Настройки и ссылки не изменялись.

### 1.5. Наблюдение нагрузки

**Идея:** запустить один контролируемый вычислительный процесс, найти его в `top`, зафиксировать нагрузку, корректно завершить и подтвердить возврат системы в исходное состояние.

**Ожидаемый результат:** известны PID, команда завершения, загрузка до/во время/после опыта; фонового процесса не осталось.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** процесс `yes > /dev/null` получил известный PID, за три секунды занял около 54% CPU по `top`, был завершён по PID и исчез из `ps`; весь прогон занял 7 секунд. Ошибка `kill 999999` воспроизведена и исправлена использованием сохранённого PID. Процесс и временный файл ошибки удалены.

### 1.6. Диагностика неверной скорости

**Идея:** подключиться к UART со скоростью, отличной от 115200 бод, наблюдать загрузку, зафиксировать искажённый вывод и восстановить правильные параметры.

**Ожидаемый результат:** студент изменяет только скорость и объясняет, почему нечитаемые символы подтверждают наличие электрического сигнала.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** при 9600 бод за 35 секунд получено 696 байт нечитаемых данных с 336 ошибками декодирования UTF-8. После возврата 115200 журнал ядра и запуск `multi-user.target` снова читаются; полный опыт занял около 80 секунд. Изменялась только скорость локального порта, журналы проверки удаляются.

## Лабораторная №2

### 2.1. Карта сетевого пути

**Идея:** изобразить путь от рабочего компьютера до Lichee через точку доступа и подписать интерфейсы, IPv4-адреса, префикс и шлюз.

**Ожидаемый результат:** каждое значение подтверждено выводом `ip -brief address` или `ip route` на соответствующей системе.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** за 3 секунды подтверждены адреса хоста и платы в одной `/24`, путь через Wi-Fi, маршрут платы через шлюз, доступность шлюза, DNS и двусторонний ICMP. Типовая ошибка при автоматическом извлечении шлюза возникла из-за экранирования `awk`; восстановление — взять адрес из строки `default via` и подставить явно. Настройки сети не изменялись.

### 2.2. Двусторонняя передача

**Идея:** передать текстовый и бинарный файлы на Lichee, проверить размер и SHA-256, изменить текстовый файл и вернуть его под новым именем.

**Ожидаемый результат:** исходные копии имеют одинаковые хеши, а изменённый удалённый файл получает новый хеш.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** переданы текстовый файл размером 70 байт и бинарный размером 32 КиБ; размеры и исходные SHA-256 совпали. После добавления строки текстовый файл вырос до 101 байта, получил новый хеш и был возвращён под новым именем. Типовая ошибка — сравнивать изменённый файл с исходным хешем; восстановление — хранить хеши до и после изменения отдельно. Все удалённые копии удалены; прогон занял около 30 секунд.

### 2.3. Псевдоним SSH

**Идея:** создать запись `Host lichee-team` в `~/.ssh/config`, указав `HostName`, `User` и `IdentityFile`, затем подключиться по псевдониму.

**Ожидаемый результат:** `ssh lichee-team` использует нужного пользователя и ключ без отключения проверки ключа сервера.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** временная конфигурация с `Host`, `HostName`, `User`, `IdentityFile`, `IdentitiesOnly` и строгой проверкой ключа обеспечила вход по `ssh -F ... lichee-team` за 7,5 секунды. Несуществующий псевдоним воспроизвёл ошибку DNS; исправление — проверить итоговую конфигурацию через `ssh -G`. Временный файл удаляется, пользовательский `~/.ssh/config` не изменялся.

### 2.4. Удалённая команда

**Идея:** без интерактивного входа получить имя пользователя, версию ядра, архитектуру и доступную память, сохранив вывод на рабочем компьютере.

**Ожидаемый результат:** студент объясняет, где исполнялись команды и какой shell выполнял перенаправление в файл.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** составная команда сохранила на хосте шесть строк с пользователем, ядром 6.16.0, архитектурой `riscv64` и памятью; выполнение заняло 7,8 секунды. Типовая ошибка — поместить локальное перенаправление внутрь кавычек удалённой команды; восстановление — закрыть кавычки перед `> отчёт.txt`. Локальный отчёт удаляется, плата не изменялась.

### 2.5. SSH-туннель

**Идея:** запустить на Lichee HTTP-сервис, привязанный к `127.0.0.1:8000`, и открыть его с рабочего компьютера через локальный порт 8080.

```bash
python3 -m http.server 8000 --bind 127.0.0.1
ssh -L 18080:127.0.0.1:8000 пользователь@IP-адрес_платы
```

**Ожидаемый результат:** страница недоступна напрямую по адресу платы, но доступна через `http://127.0.0.1:18080`; после проверки оба процесса завершены.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** сервис на `127.0.0.1:8000` доступен локально на плате и недоступен по её сетевому адресу; через туннель на свободном локальном порту 18080 получена контрольная строка. Порт 8080 оказался занят, что воспроизвело типовую ошибку; восстановление — проверить `ss -lnt` и выбрать свободный порт. Туннель, управляющий сокет, HTTP-процесс и каталог удалены; прогон занял около 40 секунд.

### 2.6. Диагностика соединения

**Идея:** найти одну обратимую неисправность: неверного пользователя или IP-адрес, остановленный `sshd`, ошибку DNS либо неправильные права `authorized_keys`.

**Ожидаемый результат:** оформлен протокол «симптом → гипотеза → команда проверки → результат → исправление»; изменяется по одному фактору за раз.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** для обратимого отказа «неверный пользователь» получено `Permission denied (publickey,password)`, после замены имени на существующее команда вернула `root`. Диагностика заняла около 15 секунд, службы и конфигурация не изменялись. Остановка `sshd` исключена из студенческого варианта как излишне рискованная.

## Лабораторная №3

Полный прогон выполнен 10.09.2026 на рабочем компьютере x86-64 и Lichee RV Dock через UART; файлы передавались по SCP.

### 3.1. Инспектор ELF

**Идея:** собрать одну программу нативным `gcc` и кросс-компилятором, затем сравнить архитектуру, тип ELF, порядок байтов, ABI, способ линковки и размер файлов.

```bash
gcc -O2 -Wall -Wextra -Wpedantic -o math-app-x86_64 math-app.c -lm
riscv64-linux-gnu-gcc -O2 -Wall -Wextra -Wpedantic -static -o math-app-riscv64 math-app.c -lm
file math-app-x86_64 math-app-riscv64
riscv64-linux-gnu-readelf -h math-app-riscv64
```

**Ожидаемый результат:** студент доказывает, какой файл запускается непосредственно на x86-64, а какой содержит код RISC-V, не опираясь на имя файла.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** менее чем за секунду собраны x86-64 PIE и статический RISC-V ELF; `file` и `readelf` подтвердили класс ELF64, little-endian, `Machine: RISC-V`, RVC, double-float ABI и статическую линковку. Типовая ошибка — судить об архитектуре по имени; восстановление — проверять содержимое через `file`. Артефакты удалены.

### 3.2. Надёжный CLI

**Идея:** расширить математическое приложение так, чтобы оно различало отсутствие аргумента, лишние символы, бесконечность, переполнение и значение вне области определения.

**Ожидаемый результат:** подготовлена таблица допустимых и ошибочных входов; для ошибки программа печатает понятное сообщение в `stderr` и возвращает ненулевой код.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** за 0,14 секунды таблицей проверены допустимое `2` и ошибки: нет аргумента, `abc`, `2x`, `inf`, `-1`, переполнение `exp(1000)`. Через QEMU и на плате получены одинаковые коды 0, 2, 3, 3, 3, 4 и 5; ошибки сопровождаются сообщениями в `stderr`. Типовая ошибка — проверять только результат `strtod`, но не остаток строки; исправление — условие `*end == '\0'`. Файлы удалены.

### 3.3. Статическая и динамическая сборки

**Идея:** собрать статический и динамический RISC-V-бинарники, сравнить размеры, ELF-интерпретатор и требования к запуску через QEMU.

```bash
riscv64-linux-gnu-gcc -O2 -Wall -Wextra -Wpedantic -static -o math-app-static math-app.c -lm
riscv64-linux-gnu-gcc -O2 -Wall -Wextra -Wpedantic -o math-app-dynamic math-app.c -lm
file math-app-static math-app-dynamic
riscv64-linux-gnu-readelf -l math-app-dynamic
```

**Ожидаемый результат:** статический файл запускается автономно, динамический без sysroot выдаёт объяснимую ошибку, а с правильным sysroot выполняется.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** статический файл размером 3 088 464 байта и динамический размером 16 184 байта собраны за 0,44 секунды. Без sysroot воспроизведена ошибка загрузчика, с sysroot запуск успешен; на плате обнаружен нужный интерпретатор и оба файла запустились. Передача заняла 11,3 секунды. Типовая ошибка и восстановление составляют наблюдаемую часть задания; файлы удалены с обеих систем.

### 3.4. Воспроизводимая сборка

**Идея:** создать минимальный `Makefile` с переменными `CC`, `CFLAGS`, `LDLIBS`, целями `all` и `clean`, выполнить чистую повторную сборку и сравнить SHA-256.

**Ожидаемый результат:** одной командой создаются оба ожидаемых файла, а `make clean` удаляет только артефакты этого проекта.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** две последовательные `make clean all` дали одинаковые SHA-256 обоих файлов; каждая сборка заняла менее секунды. Типовая ошибка — объявить `clean` обычной файловой целью; восстановление — добавить `.PHONY: all clean`. `make clean` удалил только два объявленных артефакта.

### 3.5. Системные вызовы под QEMU

**Идея:** запустить статическую программу с трассировкой QEMU и связать несколько системных вызовов с запуском, выводом и завершением программы.

```bash
qemu-riscv64-static -strace ./math-app-static 2
```

**Ожидаемый результат:** студент находит как минимум `write` и `exit_group`, объясняет их связь с программой и отличает трассировку системных вызовов от трассировки инструкций.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** менее чем за секунду получена трасса с `brk`, `mprotect`, `fstat`, `write` и `exit_group`; вывод программы сохранился. Типовая ошибка — искать вызовы в `stdout`, хотя QEMU пишет трассу в `stderr`; восстановление — перенаправить `2> trace.log`. Журнал и бинарник удалены.

### 3.6. Численная сопоставимость

**Идея:** запустить один и тот же RISC-V-файл через QEMU и на Lichee для набора входов, автоматически сравнить результаты с заданным допуском и проверить одинаковые коды возврата.

**Ожидаемый результат:** таблица содержит вход, вывод QEMU, вывод Lichee, абсолютное или относительное отклонение и решение по допуску.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** SHA-256 исполняемого файла совпал на хосте и плате. Для `0`, `0.5`, `2` и `10` вывод и коды возврата QEMU и Lichee совпали побайтно; SHA-256 файлов результатов одинаков. С переносом прогон занял около 25 секунд. Типовая ошибка — сравнить разные сборки; восстановление — сначала сверить хеш бинарника. Бинарники и результаты удалены, временный UART-сеанс остановлен, штатный `serial-getty` восстановлен.

## Лабораторная №4

Полный прогон выполнен 10.09.2026 в отдельной copy-on-write копии Linux 6.16 и внешнем каталоге сборки. Исходное дерево `/home/taranev/work_repos/kernel-source-6.16` и его готовые артефакты не изменялись. Новое ядро и DTB на плату не устанавливались.

### 4.1. Атлас артефактов ядра

**Идея:** сопоставить `.config`, `Image`, `vmlinux`, один `.ko` и DTB с их назначением, этапом использования и форматом.

**Ожидаемый результат:** для каждого объекта указаны путь, размер, `file` и подходящий способ проверки архитектуры.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** `file` определил `Image` как загрузочный образ RISC-V, DTB как Device Tree Blob, `nouveau.ko` как перемещаемый RISC-V ELF; `readelf` подтвердил `Machine: RISC-V` у `vmlinux` и модуля. Типовая ошибка — применять `readelf` к DTB; восстановление — использовать `file` и `dtc -I dtb -O dts`. Утилита `modinfo` на хосте отсутствовала, но для проверки формата она не требуется. Временный декомпилированный DTS удалён.

### 4.2. Карта зависимостей Kconfig

**Идея:** исследовать определения и зависимости `PREEMPT_RT`, `ARCH_SUNXI` и `RTW88_8723DS`, затем наблюдать нормализацию невозможного значения.

**Ожидаемый результат:** таблица содержит тип, `depends on`, `select`, назначение и фактическое значение каждого символа; невозможная комбинация объяснена по Kconfig.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** подтверждены зависимости `PREEMPT_RT` от `EXPERT`, `ARCH_SUPPORTS_RT` и отключённого `COMPILE_TEST`; `ARCH_SUNXI` от `MMU` и отсутствия `XIP_KERNEL`; `RTW88_8723DS` от `MMC` и родительских `CFG80211`, `MAC80211`, `RTW88`. При `EXPERT=n` вручную добавленный `PREEMPT_RT=y` исчез после `olddefconfig`. Прогон занял менее 10 секунд. Ошибка исправляется через зависимости, а не повторным добавлением строки; отдельный тестовый каталог удалён.

### 4.3. Полная и сокращённая конфигурации

**Идея:** сравнить полный `.config`, базовую копию, результат `savedefconfig`, обычный diff и `scripts/diffconfig`.

**Ожидаемый результат:** студент различает точный снимок состояния, переносимую конфигурационную дельту и изменения относительно выбранной базы.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** итоговый `.config` содержит 7176 строк, базовый — 6935, результат `savedefconfig` — 321, полный diff — 649, сокращённый `diffconfig` — 254 строки. Включение `EXPERT` открывает много дополнительных значений по умолчанию, поэтому diff шире четырёх целевых символов. Типовая ошибка — считать `defconfig` полной копией `.config`; восстановление — воспроизвести его через Kconfig и сравнить результат. Временные сравнения удалены.

### 4.4. Встроенный драйвер или модуль

**Идея:** сравнить `RTW88_8723DS=y` и `RTW88_8723DS=m`, связанные артефакты и требования к ранней загрузке.

**Ожидаемый результат:** при `m` получены RISC-V-модули `rtw88_8723ds.ko` и его зависимости; при `y` код включён в ядро и отдельного обязательного модуля нет.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** смена на `m` создала пять модулей RTW88, включая `rtw88_8723ds.ko` размером 6,5 КБ; `file` и `readelf` подтвердили RISC-V ELF. Попытка выполнить только `make modules` со старым `vmlinux` воспроизвела ошибки `exported twice`; согласованная инкрементальная сборка исправила состояние за 24 секунды. Затем конфигурация `y` и согласованный комплект восстановлены за 37 секунд. Модули на плату не устанавливались.

### 4.5. Внешнее дерево сборки

**Идея:** использовать `O=...`, доказать разделение исходников и результатов и безопасно диагностировать загрязнённое дерево исходников.

**Ожидаемый результат:** `.config`, объекты и артефакты находятся только в каталоге сборки; исходники остаются пригодны для другой конфигурации.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** попытка внешней сборки с ранее собранным исходным деревом дала `The source tree is not clean`. `make mrproper` без `ARCH=riscv` не удалил архитектурные generated-файлы; `make ARCH=riscv mrproper` на отдельной копии исправил ошибку. После этого `defconfig` создался за 5 секунд, а исходники остались без `.config`, `include/config` и `arch/riscv/include/generated`. Типовая опасность — очистить единственное рабочее дерево; восстановление выполняется только на копии или свежей распаковке.

### 4.6. Чистая и инкрементальная сборки

**Идея:** измерить полную и повторную сборки при фиксированной конфигурации и объяснить работу зависимостей Make.

**Ожидаемый результат:** зафиксированы команды, параллелизм, время, память, размер каталога и хеш `Image`; сравнение сделано в сопоставимых условиях.

**Текущий статус:** проверено полностью 10.09.2026.

**Результат:** отдельная чистая сборка с неизменным `-j8` заняла 21 минуту 56 секунд, повторный запуск той же команды без изменений — 9,93 секунды; ошибок компиляции не было. Каталог занял 743 МБ, `Image` — около 29 МБ, максимальный RSS за завершающий этап — около 304 МиБ, swap не использовался. Типовая ошибка — принять длительную сборку generic `defconfig` за зависание; восстановление — наблюдать журнал и процессы, не перезапуская сборку. Каталоги проверки удалены после фиксации результатов.

## Лабораторная №5

Полный прогон выполнен 17.09.2026 в отдельном временном каталоге. Использовались результаты Linux 6.16 из лабораторной №4, OpenSBI и U-Boot закреплённых ревизий, пакет ALT и архив ALT Regular для riscv64. Системные репозитории, исходное дерево ядра и рабочая microSD не изменялись; запись загрузчика и замена компонентов на плате не выполнялись.

### 5.1. Паспорт комплекта загрузки

**Идея:** составить манифест `Image`, DTB, `extlinux.conf`, OpenSBI, двух вариантов U-Boot и rootfs с назначением, источником, версией, размером, форматом и SHA-256, затем проверить комплект через `sha256sum -c`.

**Ожидаемый результат:** все исходные файлы проходят проверку, а изменение отдельной рабочей копии обнаруживается как несовпадение контрольной суммы.

**Текущий статус:** проверено полностью 17.09.2026.

**Результат:** семь SHA-256 вычислены за 6,10 секунды; проверка шести файлов подготовленного комплекта заняла 0,19 секунды и вернула статус 0. Подмена копии `extlinux.conf` была обнаружена как `ПОВРЕЖДЁН` со статусом 1. Типовая ошибка — пересчитать и заменить эталонный хеш после случайного изменения; восстановление — хранить манифест вместе с происхождением исходников и повторно взять файл из эталонного комплекта. Изменённая копия и временный манифест удалены, исходные файлы сохранили прежние хеши.

### 5.2. Аудит DTB платы

**Идея:** декомпилировать DTB, подтвердить модель и совместимость, найти CPU, UART и MMC, исследовать статусы узлов и проверить, что DTB близкой платы отвергается точной проверкой `model`.

**Ожидаемый результат:** студент доказывает принадлежность DTB по содержимому, а не имени файла, и объясняет отсутствие статического узла памяти, если DRAM описывает загрузчик.

**Текущий статус:** проверено полностью 17.09.2026.

**Результат:** менее чем за секунду подтверждены `Sipeed Lichee RV Dock`, совместимости `sipeed,lichee-rv-dock`, `sipeed,lichee-rv` и `allwinner,sun20i-d1`, CPU `rv64imafdc`, `serial0` по адресу `0x2500000` со скоростью 115200 и контроллеры MMC. В декомпилированном дереве найдены 11 явных `okay`, 21 `disabled` и два `reserved`; статического узла `memory` нет. DTB MangoPi MQ Pro воспроизвёл типовую ошибку выбора близкой платы: точная проверка модели вернула статус 1. Восстановление — вернуться к DTB из согласованной сборки ядра; временные DTS удалены.

### 5.3. Дополнение шаблона Extlinux

**Идея:** дополнить готовый `extlinux.conf` второй загрузочной записью с другой меткой, сохранив проверенные пути к ядру, DTB и rootfs, затем выбрать запись по умолчанию.

**Ожидаемый результат:** студент использует готовый шаблон без написания проверочного скрипта и объясняет назначение `default`, `timeout`, `label` и `menu label`.

**Текущий статус:** проверено полностью 17.09.2026.

**Результат:** менее чем за минуту шаблон с `default`, `timeout`, `label`, `menu label`, `kernel`, `fdt` и `append` дополнен второй записью с отдельной меткой. Обе записи использовали те же существующие `Image` и DTB; переключение `default` не потребовало изменения файлов ядра. Типовая ошибка — оставить одинаковые значения `label`, из-за чего записи нельзя однозначно выбрать; восстановление — назначить уникальные идентификаторы. После проверки дополнительная запись удалена, исходный шаблон сохранён.

### 5.4. Самостоятельный и пакетный U-Boot

**Идея:** сравнить формат, размер, SHA-256 и встроенную строку версии самостоятельно собранного образа и варианта из RPM ALT.

**Ожидаемый результат:** оба файла распознаны как загрузочные образы Allwinner D1, а различия объяснены версиями и параметрами сборки без требования одинаковых хешей.

**Текущий статус:** проверено полностью 17.09.2026.

**Результат:** сравнение заняло 0,01 секунды. Самостоятельный образ `2024.01-rc1-g2e89b706-dirty` имеет размер 963197 байт и SHA-256 `3d3d812d9786863ea5f8895c34cf7fb08af7e9f1bb2300b9f5ce5321205ca382`; пакетный `2024.01-sunxi-riscv-alt8.git8c36243b6c` — 933557 байт и `ea802b07311eadf73bfdd250899b494d6e77b4c724a3992d85b1be09d62e68e1`. Оба содержат eGON SPL размером 73728 байт. Типовая ошибка — считать различие SHA-256 признаком повреждения; восстановление — сравнить происхождение, строки версии и формат. RPM распаковывался без установки и был удалён вместе с временным каталогом.

### 5.5. Аудит архива rootfs

**Идея:** проверить поток XZ, структуру tar, версию системы, `init`, systemd, ссылки, владельцев и специальные файлы без распаковки, а затем проверить обнаружение усечённой копии.

**Ожидаемый результат:** полный архив проходит обе проверки, содержит минимально необходимые объекты, а усечённый завершается ненулевым статусом.

**Текущий статус:** проверено полностью 17.09.2026.

**Результат:** архив ALT Regular `20260316` размером 1317274236 байт и SHA-256 `3373e57c0d29a9038fd6d840c6e306f333ebba0ee95484023974f542de2c7c3d` прошёл `xz -t` за 1 минуту 10,42 секунды. Список из 106988 записей получен за 1 минуту 16,52 секунды; `/usr/sbin/init` является ссылкой на `../lib/systemd/systemd`, присутствуют устройства `/dev`. Усечённая копия за 6,16 секунды дала `Unexpected end of input` и статус 1. Типовая ошибка — ограничиться выводом `file`; восстановление — повторно получить архив и проверить XZ и tar. Полный архив сохранён неизменным, усечённая копия удалена.

### 5.6. Воспроизводимая сборка U-Boot

**Идея:** дважды выполнить чистую сборку одной ревизии с одинаковым OpenSBI, тулчейном, конфигурацией и фиксированным `SOURCE_DATE_EPOCH`, затем сравнить итоговые образы.

**Ожидаемый результат:** SHA-256 и побайтовое содержимое двух `u-boot-sunxi-with-spl.bin` совпадают; без фиксированной эпохи в строке версии видна текущая дата сборки.

**Текущий статус:** проверено полностью 17.09.2026.

**Результат:** чистые сборки с `-j8` заняли 47,75 и 42,59 секунды, максимальный RSS составил около 133 МиБ. При `SOURCE_DATE_EPOCH=1698742382` оба образа получили SHA-256 `3d3d812d9786863ea5f8895c34cf7fb08af7e9f1bb2300b9f5ce5321205ca382`, а `cmp -s` вернул статус 0; строка версии содержит UTC-время commit. До фиксации эпохи наблюдалась текущая локальная дата. Типовая ошибка — сравнивать сборки после изменения исходников или OpenSBI; восстановление — проверить commit, состояние дерева, тулчейн и все параметры. Временные деревья и дубликаты образов удалены после фиксации результатов.

## Зафиксированные результаты основной части

Следующие результаты относятся к обязательной части лабораторных и не повышают статус дополнительных заданий автоматически:

- `ssh-copy-id` успешно проверен для отдельного пользователя с домашним каталогом;
- вход по ключу подтверждён с `BatchMode=yes`;
- после `ssh-copy-id` каталог `.ssh` имел режим `700`, а `authorized_keys` — `600`;
- временный пользователь и его домашний каталог удалены после проверки;
- `sshd -t`, `systemctl is-active sshd` и проверка TCP-порта 22 прошли успешно;
- на образе отсутствует `fdisk`, поэтому в лабораторной №1 используется `lsblk -f`;
- исходный каталог `/root` отсутствовал, из-за чего первая попытка `ssh-copy-id` для root дала ложное сообщение об успехе; каталог был создан с режимом `700`, после чего вход по ключу проверен;
- статическая и динамическая сборки RISC-V, QEMU, sysroot и `-strace` проверены на x86-64-хосте;
- оба RISC-V-бинарника запущены на Lichee, их SHA-256 подтверждены, а результаты статического файла сопоставлены с QEMU;
- после проверки удалены файлы и процессы, остановлен временный UART-сеанс и восстановлен штатный `serial-getty@ttyS0`;
- полная сборка Linux 6.16 с `PREEMPT_RT=y`, `ARCH_SUNXI=y`, `RTW88_8723DS=y` завершилась успешно; получены и проверены `Image`, `Image.gz`, `vmlinux`, DTB и модули;
- текущее ядро платы через UART подтвердило те же три символа, но работающий образ использует DTB с моделью `MangoPi MQ Pro`, тогда как собранный DTB содержит `Sipeed Lichee RV Dock`; установка новых артефактов отложена до лабораторной по загрузочному носителю.
- OpenSBI `3593a5f...` собран за 11,45 секунды; `fw_dynamic.bin` имеет размер 279104 байта и SHA-256 `28ec8a3a35e44f95edb4ddbe02f99ca1aab3bc8e67920314689332aeccd9515b`;
- для U-Boot `d1-wip` подтверждена несовместимость исходного `libfdt.i_shipped` со SWIG 4.4 и проверена минимальная поправка трёх вызовов `SWIG_Python_AppendOutput`;
- пользовательский Python с `setuptools 82` скрывал системный `pkg_resources`; изолированный `setuptools 80.10.2` позволил завершить проверку без изменения системного Python;
- самостоятельно собранный и пакетный U-Boot распознаны как RISC-V eGON-образы, но ожидаемо имеют разные версии, размеры и SHA-256;
- макет BOOT с `Image`, DTB и `extlinux/extlinux.conf` проверен без записи на носитель; рабочая microSD и системные репозитории не изменялись.

## Шаблон следующей проверки

Для обновления статуса используйте один блок на попытку:

```text
Дата:
Проверяющий:
ID задания:
Стенд и версии:
Начальное состояние:
Фактические действия:
Фактический результат:
Продолжительность:
Обнаруженные проблемы:
Способ очистки или восстановления:
Решение: проверить полностью / исправить / отклонить
```
